var RESOURCES = {
  documents: require('./documents'),
  schemas: require('./schemas'),
  blobs: require('./blobs'),
  users: require('./users'),
  groups: require('./groups')
};

module.exports = RESOURCES;
